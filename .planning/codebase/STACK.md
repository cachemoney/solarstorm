# Technology Stack

**Analysis Date:** 2026-03-10

## Languages

**Primary:**
- TypeScript 5.3.0 - All source code and build configuration
- JavaScript - Runtime execution (compiled from TypeScript)
- GLSL - Shader code for WebGL rendering

## Runtime

**Environment:**
- Node.js - No version pinned (no .nvmrc file)

**Package Manager:**
- npm - Primary package manager
- Lockfile: `package-lock.json` (lockfileVersion 3)

## Frameworks

**Core:**
- React 18.2.0 - UI framework
- React Three Fiber 8.15.0 - React renderer for Three.js
- Three.js 0.160.0 - 3D graphics library

**State Management:**
- Zustand 4.4.0 - Lightweight state management store

**Build/Dev:**
- Vite 7.3.1 - Frontend build tool and dev server
- Vite Plugin React 5.1.4 - React plugin for Vite
- Vite Plugin GLSL 1.2.0 - GLSL shader loader for Vite

## Key Dependencies

**Critical:**
- @react-three/fiber 8.15.0 - Bridges React and Three.js for 3D rendering
- @react-three/drei 9.90.0 - Utility components for React Three Fiber (OrbitControls, CameraShake, useGLTF)
- three 0.160.0 - WebGL rendering engine with audio support (AudioLoader, AudioListener, AudioAnalyser, Audio)

**Graphics & Effects:**
- glslify 7.1.1 - GLSL module bundler and processor
- glsl-noise 0.0.0 - GLSL noise functions
- glsl-blend-soft-light 1.0.5 - GLSL soft light blending shader
- glsl-film-grain 1.0.4 - GLSL film grain effect shader
- canvas-sketch-util 1.10.0 - Math utilities (mapRange function)

**Development:**
- @types/react 18.2.0 - TypeScript definitions for React
- @types/react-dom 18.2.0 - TypeScript definitions for React DOM
- @types/three 0.160.0 - TypeScript definitions for Three.js
- puppeteer 24.37.5 - Headless browser automation (likely for screenshot generation)

## Configuration

**Environment:**
- No environment variables required - all configuration is hardcoded in source
- No .env files present

**Build:**
- `vite.config.ts` - Vite configuration with React and GLSL plugin setup
- `tsconfig.json` - TypeScript compiler options targeting ESNext, with path alias `@/*` → `src/*`
- `tsconfig.node.json` - Separate config for Vite build file
- `.prettierrc` - Prettier code formatting: single quotes, bracket spacing enabled, JSX bracket on same line

## Platform Requirements

**Development:**
- Node.js (version not specified)
- npm or compatible package manager
- Modern browser with WebGL support

**Production:**
- Static hosting (Netlify specified in HTML meta tags)
- Browser with WebGL and Web Audio API support
- HTTPS required for audio playback (Web Audio API security requirement)

## Static Assets

**Hosting:**
- Audio files: `/public/quitters-raga/` directory (bass.mp3, drums.mp3, melody.mp3, vocals.mp3, quitters-raga.mp3)
- 3D model: `/public/model.gltf` - GLTF format model file
- Images: Screenshot PNGs, OpenGraph image for social sharing

## Build Output

**Distribution:**
- `dist/` directory - Generated by Vite build process
- Deployed to: solarstorm.netlify.app (Netlify)

---

*Stack analysis: 2026-03-10*
